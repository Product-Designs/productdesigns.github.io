---
layout: base.njk
title: Product-Design Archives
subtitle: project database // experimental prototypes
description: Welcome to the product-design terminal interface.
altDescription: Browse experimental prototypes and side projects from the wasteland of creative explorations.
---

<div class="crt-overlay"></div>
<div class="theme-switcher">
    <div class="theme-btn green active" id="themeGreen" title="Vault-Tec Green"></div>
    <div class="theme-btn orange" id="themeOrange" title="Wasteland Orange"></div>
</div>
<div class="header">
    <h1>{{ title }}</h1>
    <p class="subtitle">{{ subtitle }}</p>
    <p class="description">
        {{ description }} <br/> {{ altDescription }}
    </p>
</div>
<div class="terminal-interface">
    <div class="terminal-menu">
        <div class="corner-frame">
            <ul class="menu-list" id="projectList">
                {% for design in designs %}
                    <li class="menu-item {% if loop.first %}active{% endif %}" data-index="{{ loop.index0 }}" data-title="{{ design.title }}" data-description="{{ design.detailedDescription }}" data-meta='{{ design.meta | dump | safe }}'>
                        {{ design.title }}
                    </li>
                {% endfor %}
            </ul>
        </div>
    </div>
    <div class="terminal-content">
        <div class="corner-frame detail-frame">
            <div id="contentArea">
                <h2 class="content-title" id="activeTitle">{{ designs[0].title }}</h2>
                <p class="content-description" id="activeDescription">{{ designs[0].detailedDescription }}</p>
            </div>
            <div class="terminal-footer">
                <div class="meta-box corner-frame" id="metaType">
                    <span class="label">TYPE:</span>
                    <span class="value" id="valType"></span>
                </div>
                <div class="meta-box corner-frame" id="metaRole">
                    <span class="label">ROLE:</span>
                    <span class="value" id="valRole"></span>
                </div>
                <div class="meta-box corner-frame" id="metaYear">
                    <span class="label">YEAR:</span>
                    <span class="value" id="valYear"></span>
                </div>
            </div>
        </div>
    </div>
</div>